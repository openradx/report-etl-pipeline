run_coordinator:
  module: dagster.core.run_coordinator
  class: QueuedRunCoordinator
  config:
    max_concurrent_runs: 3

storage:
  postgres:
    postgres_db:
      username: postgres
      password: postgres
      hostname: postgres
      db_name: postgres
      port: 5432

telemetry:
  enabled: false

nux:
  enabled: false
