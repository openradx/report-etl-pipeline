run_coordinator:
  module: dagster.core.run_coordinator
  class: QueuedRunCoordinator
  config:
    max_concurrent_runs: 3

# run_retries:
#   enabled: true
#   max_retries: 3

telemetry:
  enabled: false

nux:
  enabled: false
