version: '3.7'

services:
  dagster:
    build:
      target: production
    ports:
      - 3000:3000
    volumes:
      - dagster_home:/opt/dagster/dagster_home
    command: dagster dev

volumes:
  dagster_home:
